- add a gene definition finder (e.g. "PITX2 is responsible for", "OTX2 encodes", "PITX2 is a", "TP53 regulates")
- make pubLoadMysql updateable ("loadedFiles": dataset, updateId, fileCount, fsizeSum?)
- make pubRunAnnot updateable (getUpdateIds()...)

